# SlateZero
A minimal Notion-style rich text editor built from scratch in TypeScript, featuring block-based editing, inline command menus, and extensible component-based architecture — without relying on external rich text libraries.
